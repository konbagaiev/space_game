{
  "name": "space-game-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Three.js game client. Pure logic lives in src/ and is unit-tested with node:test.",
  "scripts": {
    "test": "node --test",
    "test:visual": "node visual/run.mjs",
    "bench": "node bench/run.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
